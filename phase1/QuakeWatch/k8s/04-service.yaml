apiVersion: v1
kind: Service
metadata:
  name: quakewatch-service
  namespace: quakewatch
  labels:
    app: quakewatch
spec:
  type: NodePort
  selector:
    app: quakewatch
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30081
